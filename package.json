{
  "name": "yield-adapter-standard",
  "private": true,
  "version": "0.1.0",
  "description": "Reference implementation of a Solana Yield Adapter Standard (dispatcher + registry + 5 adapters + SDK + conformance suite).",
  "license": "MIT",
  "scripts": {
    "verify-addresses": "tsx scripts/verify-addresses.ts",
    "inspect-idls": "tsx scripts/inspect-idls.ts",
    "build": "bash scripts/build.sh",
    "test": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.spec.ts",
    "test:fork": "bash scripts/fork-test.sh",
    "test:rust": "bash scripts/test-rust.sh",
    "probe:drift-if": "tsx scripts/probe-drift-if.ts",
    "deploy:devnet": "bash scripts/deploy-devnet.sh",
    "verify:devnet": "ANCHOR_PROVIDER_URL=https://api.devnet.solana.com tsx scripts/verify-devnet.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "chai": "^4.5.0",
    "dotenv": "^16.4.5",
    "mocha": "^10.7.0",
    "ts-mocha": "^10.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@anchor-lang/core": "^1.0.2",
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.95.3"
  }
}
